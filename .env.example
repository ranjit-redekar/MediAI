# Leave both unset to run the demo (mock data, role picker, no backend).
# Set both to run against Supabase. Local values come from `npx supabase status`.
VITE_SUPABASE_URL=http://127.0.0.1:54421
VITE_SUPABASE_PUBLISHABLE_KEY=
